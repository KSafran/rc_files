require('fzf-lua').setup({
    -- files = {
    --     fd_opts =
    --     "--type f --hidden --exclude .git --exclude node_modules --exclude fronted/node_modules --exclude venv --exclude backend/venv",
    -- },
    -- grep = {
    --     rg_opts =
    --     "--hidden --column --line-number --no-heading --color=always --smart-case --no-ignore-vcs --glob '!venv/**'",
    -- },
})
